import styled from 'styled-components';
import theme from '../theme/main';

export const TeamActions = styled.div`
    border-top: 1px dashed ${theme.colorBlackTransparent};
    margin-top: 1rem;
    padding: 1rem;
`;
